diff --git a/client/src/components/QuotationsTab.tsx b/client/src/components/QuotationsTab.tsx
--- a/client/src/components/QuotationsTab.tsx
+++ b/client/src/components/QuotationsTab.tsx
@@ -664,6 +664,7 @@
   function detectQuotationLibraryCategory(q: Quotation): QuotationServiceCategory | null {
     const source = [
+      getServiceTitle(q),
       q.title || "",
       ...q.items.map((item) => item.service || ""),
       q.servicesDescription || "",
     ].join(" ").toLowerCase();
@@ -706,11 +707,8 @@
   function openMatchingServiceLibraryFromPreview() {
     if (!current) return;
 
     const category = detectQuotationLibraryCategory(current);
-    if (!category) {
-      toast.error(isRTL ? "تعذر تحديد نوع الخدمة من العرض" : "Could not detect service category");
-      return;
-    }
+    if (!category) return;
 
     setLibraryCategory(category);
     setLibrarySearch("");
@@ -1088,8 +1086,8 @@
                 <div className="flex-1 overflow-y-auto bg-slate-50" style={{ minWidth: 0 }}>
                   <div className="max-w-2xl mx-auto px-6 py-6 space-y-4">
 
                     {/* 1. Template */}
-                    <div id="quotation-editor-line-items" className="scroll-mt-4 rounded-2xl border border-slate-200 bg-white shadow-sm p-5">
+                    <div className="scroll-mt-4 rounded-2xl border border-slate-200 bg-white shadow-sm p-5">
                       <p className="text-[13px] font-bold text-slate-700 mb-3 flex items-center gap-2">
                         <span className="w-5 h-5 rounded-md bg-indigo-100 flex items-center justify-center text-indigo-600 text-[10px] font-black">1</span>
                         <Palette size={14} className="text-indigo-500" />
@@ -1195,7 +1193,7 @@
                     </div>
 
                     {/* 3. Line Items */}
-                    <div className="rounded-2xl border border-slate-200 bg-white shadow-sm p-5">
+                    <div id="quotation-editor-line-items" className="scroll-mt-4 rounded-2xl border border-slate-200 bg-white shadow-sm p-5">
                       <p className="text-[13px] font-bold text-slate-700 mb-3 flex items-center gap-2">
                         <span className="w-5 h-5 rounded-md bg-green-100 flex items-center justify-center text-green-600 text-[10px] font-black">4</span>
                         {isRTL ? "بنود التسعير" : "Line Items"}
