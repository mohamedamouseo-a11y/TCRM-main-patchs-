diff --git a/shared/landingPageIntegration.types.ts b/shared/landingPageIntegration.types.ts
--- a/shared/landingPageIntegration.types.ts
+++ b/shared/landingPageIntegration.types.ts
@@ -96,8 +96,9 @@
 export const publicLeadPayloadSchema = z.object({
   name: z.string().optional(),
   phone: z.string().min(5),
   businessType: z.string().optional(),
+  businessProfile: z.string().optional(),
   acquisitionChannels: z.array(z.string()).optional(),
   budget: z.string().optional(),
   challenge: z.string().optional(),
   decisionMaker: z.string().optional(),
diff --git a/server/services/landingPageLeadIntakeService.ts b/server/services/landingPageLeadIntakeService.ts
--- a/server/services/landingPageLeadIntakeService.ts
+++ b/server/services/landingPageLeadIntakeService.ts
@@ -203,8 +203,12 @@
   const leadId = await createLead({
     name: payload.name || null,
     phone,
-    businessProfile: payload.businessLink || null,
+    businessProfile:
+      payload.businessProfile ||
+      payload.businessType ||
+      payload.businessLink ||
+      null,
     campaignName: payload.utm_campaign || payload.pageTitle || integration.name,
     ownerId: ownerId ?? null,
     stage: integration.defaultStage || "New",
     sourceId: null,
