diff --git a/server/services/tcrmDatabaseBackup.ts b/server/services/tcrmDatabaseBackup.ts
--- a/server/services/tcrmDatabaseBackup.ts
+++ b/server/services/tcrmDatabaseBackup.ts
@@ -1,3 +1,3 @@
 async function retention(drive:any,f:string){const r=await drive.files.list({q:`'${f}' in parents and trashed=false and name contains 'TCRM_DB_'`,orderBy:"createdTime desc",fields:"files(id,name)",pageSize:100});for(const x of (r.data.files||[]).slice(KEEP))if(x.id)await drive.files.delete({fileId:x.id});}
-export async function saveTcrmDbBackupSettings(input:any){const c=await config(),g=input.googleDrive||{};c.enabled=!!input.enabled;if(input.scheduleTime){if(!/^([01]\d|2[0-3]):[0-5]\d$/.test(input.scheduleTime))throw new TcrmDbBackupError("Invalid schedule time",400);c.scheduleTime=input.scheduleTime;}if(input.timeZone){new Intl.DateTimeFormat("en-US",{timeZone:input.timeZone}).format();c.timeZone=input.timeZone;}for(const k of ["clientId","redirectUri","folderId","folderName"])if(k in g)c.googleDrive[k]=clean(g[k],1000);if(clean(g.clientSecret))c.googleDrive.clientSecretEnc=enc(clean(g.clientSecret));await writeJson(CFG(),c);return publicCfg(c);}
+export async function saveTcrmDbBackupSettings(input:any){const c=await config(),g=input.googleDrive||{};c.enabled=!!input.enabled;if(input.scheduleTime){if(!/^([01]\d|2[0-3]):[0-5]\d$/.test(input.scheduleTime))throw new TcrmDbBackupError("Invalid schedule time",400);c.scheduleTime=input.scheduleTime;}if(input.timeZone){new Intl.DateTimeFormat("en-US",{timeZone:input.timeZone}).format();c.timeZone=input.timeZone;}for(const k of ["clientId","redirectUri","folderId","folderName"])if(k in g)c.googleDrive[k]=clean(g[k],1000);if(g.updateClientSecret===true&&clean(g.clientSecret))c.googleDrive.clientSecretEnc=enc(clean(g.clientSecret));await writeJson(CFG(),c);return publicCfg(c);}
 export async function getTcrmDbBackupStatus(){const c=await config();return {config:publicCfg(c),state:await state(),backups:await listDrive(),retentionCount:KEEP};}
diff --git a/server/routes/tcrmDatabaseBackup.ts b/server/routes/tcrmDatabaseBackup.ts
--- a/server/routes/tcrmDatabaseBackup.ts
+++ b/server/routes/tcrmDatabaseBackup.ts
@@ -2,6 +2,7 @@ export function createTcrmDbBackupRouter({authenticateRequest}:{authenticateRequest:(req:any)=>Promise<any>}) {
   const r=express.Router();
   r.use(async(req,res,next)=>{const u=await authenticateRequest(req).catch(()=>null);if(!u)return res.status(401).send("Authentication required");if(isDeveloperRole(u.role)||!isAdminRole(u.role))return res.status(403).send("Admin access required");(req as any).backupActor=u;next();});
   const sendErr=(res:any,e:any)=>res.status(Number(e?.statusCode)||500).json({error:e?.message||"Database backup error"});
+  r.use((req,res,next)=>{if(req.method==="GET"&&req.path==="/"){const originalSend=res.send.bind(res);(res as any).send=(body:any)=>{if(typeof body==="string"&&body.includes("TCRM Database Backup")){body+=`<script>(()=>{const cs=document.getElementById("cs"),save=document.getElementById("save"),conn=document.getElementById("conn");if(!cs||!save||!conn)return;let secretEdit=false;cs.value="";cs.readOnly=true;cs.autocomplete="new-password";cs.placeholder="Stored securely - click Change Secret to replace";const change=document.createElement("button");change.type="button";change.textContent="Change Secret";change.style.marginLeft="8px";cs.parentElement?.appendChild(change);change.onclick=()=>{secretEdit=true;cs.readOnly=false;cs.value="";cs.focus()};save.onclick=async()=>{try{const x=p();x.googleDrive.updateClientSecret=secretEdit;if(!secretEdit)x.googleDrive.clientSecret="";await api("/settings",{method:"PUT",body:JSON.stringify(x)});cs.value="";cs.readOnly=true;secretEdit=false;await load()}catch(e){q("m").textContent=e.message}};conn.onclick=async()=>{try{cs.value="";const u=await api("/google-drive/auth-url");window.top.location.href=u.url}catch(e){q("m").textContent=e.message}};setTimeout(()=>{if(!secretEdit)cs.value=""},100);setTimeout(()=>{if(!secretEdit)cs.value=""},1000)})();</script>`;}return originalSend(body)};}next();});
   r.get("/",async(_req,res)=>{res.type("html").send(`<!doctype html><title>TCRM Database Backup</title><script>load()</script>`);});
   r.get("/status",async(_q,s)=>{});
 }
