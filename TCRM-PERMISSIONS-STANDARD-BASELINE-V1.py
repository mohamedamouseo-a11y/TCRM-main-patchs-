#!/usr/bin/env python3
from pathlib import Path

ROOT = Path.cwd()
REQUIRED = [
    ROOT / "package.json",
    ROOT / "server" / "security" / "permissionCatalog.ts",
    ROOT / "server" / "security" / "permissionEngine.ts",
]
for required in REQUIRED:
    if not required.exists():
        raise SystemExit(f"ERROR: expected TCRM project file missing: {required}")

FILES = {
    "server/security/systemRolePermissionBaselines.ts": 'import type { PermissionKey, PermissionScope } from "./permissionCatalog";\n\nexport type StandardPermissionGrant = {\n  permissionKey: PermissionKey;\n  dataScope: PermissionScope;\n};\n\nconst grant = (permissionKey: PermissionKey, dataScope: PermissionScope): StandardPermissionGrant => ({\n  permissionKey,\n  dataScope,\n});\n\nexport const FULL_ACCESS_SYSTEM_ROLES = new Set(["Admin", "Developer"]);\n\n/**\n * Roles that already have a production-approved standard matrix.\n * The baseline migration preserves their current direct grants when configured,\n * and only uses the fallback below when a role has zero direct grants.\n */\nexport const PRESERVE_CONFIGURED_SYSTEM_ROLES = new Set([\n  "SalesManager",\n  "SalesAgent",\n  "ColdSalesAgent",\n]);\n\nexport const SYSTEM_ROLE_STANDARD_BASELINES: Record<string, readonly StandardPermissionGrant[]> = {\n  SalesManager: [\n    grant("dashboard.view", "team"),\n    grant("leads.view", "team"),\n    grant("leads.create", "team"),\n    grant("leads.edit", "team"),\n    grant("leads.assign", "team"),\n    grant("leads.reassign", "team"),\n    grant("deals.view", "team"),\n    grant("deals.create", "team"),\n    grant("deals.edit", "team"),\n    grant("clients.view", "team"),\n    grant("clients.edit", "team"),\n    grant("activities.view", "team"),\n    grant("activities.create", "team"),\n    grant("activities.edit", "team"),\n    grant("tasks.view", "team"),\n    grant("tasks.create", "team"),\n    grant("tasks.edit", "team"),\n    grant("tasks.assign", "team"),\n    grant("meetings.view", "team"),\n    grant("meetings.create", "team"),\n    grant("meetings.edit", "team"),\n    grant("contracts.view", "team"),\n    grant("files.view", "team"),\n    grant("files.upload", "team"),\n    grant("files.edit", "team"),\n    grant("files.share", "team"),\n    grant("reports.view", "team"),\n  ],\n\n  SalesAgent: [\n    grant("dashboard.view", "own"),\n    grant("leads.view", "assigned"),\n    grant("leads.create", "own"),\n    grant("leads.edit", "assigned"),\n    grant("deals.view", "assigned"),\n    grant("deals.create", "own"),\n    grant("deals.edit", "assigned"),\n    grant("clients.view", "assigned"),\n    grant("clients.create", "own"),\n    grant("clients.edit", "assigned"),\n    grant("activities.view", "assigned"),\n    grant("activities.create", "own"),\n    grant("activities.edit", "assigned"),\n    grant("tasks.view", "assigned"),\n    grant("tasks.create", "own"),\n    grant("tasks.edit", "assigned"),\n    grant("meetings.view", "own"),\n    grant("meetings.create", "own"),\n    grant("whatsapp.view", "own"),\n    grant("whatsapp.send", "own"),\n    grant("files.view", "assigned"),\n    grant("files.upload", "assigned"),\n    grant("files.edit", "assigned"),\n    grant("files.share", "assigned"),\n  ],\n\n  ColdSalesAgent: [\n    grant("dashboard.view", "own"),\n    grant("leads.view", "assigned"),\n    grant("leads.edit", "assigned"),\n    grant("clients.view", "assigned"),\n    grant("activities.view", "assigned"),\n    grant("activities.create", "own"),\n    grant("activities.edit", "assigned"),\n    grant("tasks.view", "assigned"),\n    grant("tasks.create", "own"),\n    grant("tasks.edit", "assigned"),\n  ],\n\n  AccountManager: [\n    grant("deals.view", "assigned"),\n    grant("clients.view", "assigned"),\n    grant("clients.edit", "assigned"),\n    grant("activities.view", "assigned"),\n    grant("activities.create", "assigned"),\n    grant("activities.edit", "assigned"),\n    grant("tasks.view", "assigned"),\n    grant("tasks.create", "assigned"),\n    grant("tasks.edit", "assigned"),\n    grant("tasks.delete", "assigned"),\n    grant("tasks.assign", "assigned"),\n    grant("meetings.view", "own"),\n    grant("meetings.create", "own"),\n    grant("meetings.edit", "own"),\n    grant("contracts.view", "assigned"),\n    grant("contracts.create", "assigned"),\n    grant("contracts.edit", "assigned"),\n    grant("whatsapp.view", "own"),\n    grant("whatsapp.send", "own"),\n    grant("messenger.view", "own"),\n    grant("messenger.send", "own"),\n    grant("files.view", "assigned"),\n    grant("files.upload", "assigned"),\n    grant("files.edit", "assigned"),\n    grant("files.delete", "assigned"),\n    grant("files.share", "assigned"),\n    grant("notifications.view", "all"),\n  ],\n\n  AccountManagerLead: [\n    grant("deals.view", "team"),\n    grant("clients.view", "team"),\n    grant("clients.edit", "team"),\n    grant("activities.view", "team"),\n    grant("activities.create", "team"),\n    grant("activities.edit", "team"),\n    grant("tasks.view", "team"),\n    grant("tasks.create", "team"),\n    grant("tasks.edit", "team"),\n    grant("tasks.delete", "team"),\n    grant("tasks.assign", "team"),\n    grant("meetings.view", "team"),\n    grant("meetings.create", "team"),\n    grant("meetings.edit", "team"),\n    grant("meetings.delete", "team"),\n    grant("contracts.view", "team"),\n    grant("contracts.create", "team"),\n    grant("contracts.edit", "team"),\n    grant("whatsapp.view", "team"),\n    grant("whatsapp.send", "team"),\n    grant("messenger.view", "team"),\n    grant("messenger.send", "team"),\n    grant("files.view", "team"),\n    grant("files.upload", "team"),\n    grant("files.edit", "team"),\n    grant("files.delete", "team"),\n    grant("files.share", "team"),\n    grant("reports.view", "team"),\n    grant("notifications.view", "all"),\n  ],\n\n  TechnicalAccountManager: [\n    grant("deals.view", "assigned"),\n    grant("clients.view", "assigned"),\n    grant("clients.edit", "assigned"),\n    grant("activities.view", "assigned"),\n    grant("activities.create", "assigned"),\n    grant("activities.edit", "assigned"),\n    grant("tasks.view", "assigned"),\n    grant("tasks.create", "assigned"),\n    grant("tasks.edit", "assigned"),\n    grant("tasks.assign", "assigned"),\n    grant("meetings.view", "own"),\n    grant("meetings.create", "own"),\n    grant("meetings.edit", "own"),\n    grant("contracts.view", "assigned"),\n    grant("whatsapp.view", "own"),\n    grant("whatsapp.send", "own"),\n    grant("messenger.view", "own"),\n    grant("messenger.send", "own"),\n    grant("files.view", "assigned"),\n    grant("files.upload", "assigned"),\n    grant("files.edit", "assigned"),\n    grant("files.share", "assigned"),\n    grant("integrations.view", "all"),\n    grant("notifications.view", "all"),\n  ],\n\n  MediaBuyer: [\n    grant("dashboard.view", "team"),\n    grant("clients.view", "assigned"),\n    grant("tasks.view", "team"),\n    grant("tasks.create", "team"),\n    grant("tasks.edit", "team"),\n    grant("campaigns.view", "team"),\n    grant("campaigns.create", "team"),\n    grant("campaigns.edit", "team"),\n    grant("files.view", "team"),\n    grant("files.upload", "team"),\n    grant("files.edit", "team"),\n    grant("files.share", "team"),\n    grant("reports.view", "team"),\n    grant("integrations.view", "all"),\n    grant("notifications.view", "all"),\n  ],\n\n  BusinessDeveloper: [\n    grant("leads.view", "own"),\n    grant("leads.create", "own"),\n    grant("leads.edit", "own"),\n    grant("deals.view", "own"),\n    grant("deals.create", "own"),\n    grant("deals.edit", "own"),\n    grant("clients.view", "own"),\n    grant("clients.create", "own"),\n    grant("activities.view", "own"),\n    grant("activities.create", "own"),\n    grant("activities.edit", "own"),\n    grant("tasks.view", "own"),\n    grant("tasks.create", "own"),\n    grant("tasks.edit", "own"),\n    grant("meetings.view", "own"),\n    grant("meetings.create", "own"),\n    grant("meetings.edit", "own"),\n    grant("files.view", "own"),\n    grant("files.upload", "own"),\n    grant("files.edit", "own"),\n    grant("files.share", "own"),\n    grant("notifications.view", "all"),\n  ],\n\n  Moderator: [\n    grant("tara.view", "all"),\n    grant("tara.operate", "all"),\n    grant("tara.moderate", "all"),\n    grant("whatsapp.view", "team"),\n    grant("whatsapp.send", "team"),\n    grant("messenger.view", "team"),\n    grant("messenger.send", "team"),\n    grant("notifications.view", "all"),\n  ],\n\n  Viewer: [\n    grant("dashboard.view", "team"),\n    grant("leads.view", "team"),\n    grant("deals.view", "team"),\n    grant("clients.view", "team"),\n    grant("activities.view", "team"),\n    grant("tasks.view", "team"),\n    grant("meetings.view", "team"),\n    grant("contracts.view", "team"),\n    grant("campaigns.view", "team"),\n    grant("whatsapp.view", "team"),\n    grant("messenger.view", "team"),\n    grant("files.view", "team"),\n    grant("reports.view", "team"),\n    grant("notifications.view", "all"),\n  ],\n};\n\nexport const STANDARD_SYSTEM_ROLE_KEYS = [\n  "Admin",\n  "Developer",\n  "SalesManager",\n  "SalesAgent",\n  "ColdSalesAgent",\n  "TechnicalAccountManager",\n  "Viewer",\n  "MediaBuyer",\n  "AccountManager",\n  "AccountManagerLead",\n  "BusinessDeveloper",\n  "Moderator",\n] as const;\n',
    "scripts/apply-system-role-standard-baselines.ts": 'import { createHash } from "node:crypto";\nimport { writeFile } from "node:fs/promises";\nimport { sql } from "drizzle-orm";\nimport { getDb } from "../server/db";\nimport { PERMISSION_SCOPES, PHASE1_PERMISSION_CATALOG, type PermissionScope } from "../server/security/permissionCatalog";\nimport {\n  FULL_ACCESS_SYSTEM_ROLES,\n  PRESERVE_CONFIGURED_SYSTEM_ROLES,\n  STANDARD_SYSTEM_ROLE_KEYS,\n  SYSTEM_ROLE_STANDARD_BASELINES,\n  type StandardPermissionGrant,\n} from "../server/security/systemRolePermissionBaselines";\n\nconst APPLY = process.argv.includes("--apply");\nconst VERSION = "TCRM_SYSTEM_ROLE_STANDARD_BASELINE_V1";\n\nfunction rows(result: any): any[] {\n  if (Array.isArray(result) && Array.isArray(result[0])) return result[0];\n  if (Array.isArray(result)) return result;\n  return [];\n}\n\nfunction canonicalHash(value: unknown) {\n  return createHash("sha256").update(JSON.stringify(value)).digest("hex");\n}\n\nfunction normalizeRow(row: any) {\n  const output: Record<string, unknown> = {};\n  for (const key of Object.keys(row).sort()) {\n    const value = row[key];\n    output[key] = value instanceof Date ? value.toISOString() : value;\n  }\n  return output;\n}\n\nfunction snapshotHash(items: any[]) {\n  return canonicalHash(items.map(normalizeRow));\n}\n\nfunction desiredForRole(roleKey: string): StandardPermissionGrant[] {\n  if (FULL_ACCESS_SYSTEM_ROLES.has(roleKey)) {\n    return PHASE1_PERMISSION_CATALOG.map(permissionKey => ({\n      permissionKey,\n      dataScope: "all" as PermissionScope,\n    }));\n  }\n  return [...(SYSTEM_ROLE_STANDARD_BASELINES[roleKey] || [])];\n}\n\nfunction validateBaseline() {\n  const catalog = new Set(PHASE1_PERMISSION_CATALOG.map(String));\n  const scopes = new Set(PERMISSION_SCOPES.map(String));\n  const errors: string[] = [];\n\n  for (const roleKey of STANDARD_SYSTEM_ROLE_KEYS) {\n    if (!FULL_ACCESS_SYSTEM_ROLES.has(roleKey) && !SYSTEM_ROLE_STANDARD_BASELINES[roleKey]) {\n      errors.push(`${roleKey}: missing baseline`);\n      continue;\n    }\n\n    const seen = new Set<string>();\n    for (const grant of desiredForRole(roleKey)) {\n      const key = String(grant.permissionKey);\n      if (!catalog.has(key)) errors.push(`${roleKey}: unknown permission ${key}`);\n      if (!scopes.has(String(grant.dataScope))) errors.push(`${roleKey}: invalid scope ${grant.dataScope}`);\n      if (seen.has(key)) errors.push(`${roleKey}: duplicate permission ${key}`);\n      seen.add(key);\n    }\n  }\n\n  if (errors.length) {\n    throw new Error(`Invalid standard baseline:\\n${errors.join("\\n")}`);\n  }\n}\n\nasync function loadSystemRoles(executor: any) {\n  return rows(await executor.execute(sql`\n    SELECT id, legacy_role_key AS legacyRoleKey, role_key AS roleKey, name, is_system AS isSystem\n    FROM roles\n    WHERE is_system = 1\n    ORDER BY id\n  `));\n}\n\nasync function loadDirectPermissions(executor: any) {\n  return rows(await executor.execute(sql`\n    SELECT r.id AS roleId, r.legacy_role_key AS legacyRoleKey,\n           p.permission_key AS permissionKey, rp.effect, rp.data_scope AS dataScope,\n           rp.scope_config AS scopeConfig\n    FROM roles r\n    JOIN role_permissions rp ON rp.role_id = r.id\n    JOIN permissions p ON p.id = rp.permission_id\n    WHERE r.is_system = 1\n    ORDER BY r.id, p.permission_key\n  `));\n}\n\nasync function protectionSnapshot(executor: any) {\n  const customRolePermissions = rows(await executor.execute(sql`\n    SELECT r.id AS roleId, rp.permission_id AS permissionId, rp.effect,\n           rp.data_scope AS dataScope, rp.scope_config AS scopeConfig\n    FROM roles r\n    JOIN role_permissions rp ON rp.role_id = r.id\n    WHERE r.is_system = 0\n    ORDER BY r.id, rp.permission_id\n  `));\n\n  const userOverrides = rows(await executor.execute(sql`\n    SELECT id, user_id AS userId, permission_id AS permissionId, effect,\n           data_scope AS dataScope, scope_config AS scopeConfig,\n           starts_at AS startsAt, expires_at AS expiresAt, reason, created_by AS createdBy\n    FROM user_permission_overrides\n    ORDER BY id\n  `));\n\n  const roleHierarchy = rows(await executor.execute(sql`\n    SELECT id, parent_role_id AS parentRoleId, is_active AS isActive\n    FROM roles\n    ORDER BY id\n  `));\n\n  return {\n    customRolePermissions: snapshotHash(customRolePermissions),\n    userOverrides: snapshotHash(userOverrides),\n    roleHierarchy: snapshotHash(roleHierarchy),\n  };\n}\n\nfunction groupByRole(items: any[]) {\n  const map = new Map<string, any[]>();\n  for (const item of items) {\n    const key = String(item.legacyRoleKey || "");\n    const bucket = map.get(key) || [];\n    bucket.push(item);\n    map.set(key, bucket);\n  }\n  return map;\n}\n\nfunction summarize(items: any[]) {\n  const grouped = groupByRole(items);\n  return Object.fromEntries(\n    STANDARD_SYSTEM_ROLE_KEYS.map(roleKey => [\n      roleKey,\n      (grouped.get(roleKey) || []).filter(item => String(item.effect) === "allow").length,\n    ]),\n  );\n}\n\nfunction dangerousNonAdminGrant(permissionKey: string) {\n  return (\n    permissionKey.startsWith("roles.") ||\n    permissionKey === "users.create" ||\n    permissionKey === "users.edit" ||\n    permissionKey === "users.delete" ||\n    permissionKey === "users.assign_roles" ||\n    permissionKey === "settings.edit" ||\n    permissionKey === "integrations.manage" ||\n    permissionKey.startsWith("backup.") ||\n    permissionKey.startsWith("developer.")\n  );\n}\n\nasync function main() {\n  validateBaseline();\n\n  const db: any = await getDb();\n  if (!db) throw new Error("DATABASE_URL is required");\n\n  const roles = await loadSystemRoles(db);\n  const byLegacy = new Map(roles.map((role: any) => [String(role.legacyRoleKey || ""), role]));\n  const missingSystemRoles = STANDARD_SYSTEM_ROLE_KEYS.filter(roleKey => !byLegacy.has(roleKey));\n\n  const permissionRows = rows(await db.execute(sql`\n    SELECT id, permission_key AS permissionKey, module_key AS moduleKey, action_key AS actionKey, is_active AS isActive\n    FROM permissions\n    ORDER BY permission_key\n  `));\n  const dbPermissionKeys = new Set(permissionRows.filter((item: any) => Number(item.isActive) === 1).map((item: any) => String(item.permissionKey)));\n  const missingCatalogPermissions = PHASE1_PERMISSION_CATALOG.map(String).filter(key => !dbPermissionKeys.has(key));\n\n  const beforeDirect = await loadDirectPermissions(db);\n  const beforeByRole = groupByRole(beforeDirect);\n  const beforeProtection = await protectionSnapshot(db);\n\n  const plans = STANDARD_SYSTEM_ROLE_KEYS\n    .filter(roleKey => byLegacy.has(roleKey))\n    .map(roleKey => {\n      const current = beforeByRole.get(roleKey) || [];\n      const currentAllows = current.filter(item => String(item.effect) === "allow");\n      const currentDenies = current.filter(item => String(item.effect) === "deny");\n      const preserveConfigured = PRESERVE_CONFIGURED_SYSTEM_ROLES.has(roleKey) && current.length > 0;\n\n      if (preserveConfigured) {\n        return {\n          roleKey,\n          mode: "preserve-configured" as const,\n          currentCount: current.length,\n          desiredCount: current.length,\n          entries: [] as Array<StandardPermissionGrant & { effect: "allow" | "deny"; scopeConfig?: unknown }>,\n        };\n      }\n\n      const desiredMap = new Map<string, StandardPermissionGrant & { effect: "allow" | "deny"; scopeConfig?: unknown }>();\n      for (const grant of desiredForRole(roleKey)) {\n        desiredMap.set(String(grant.permissionKey), { ...grant, effect: "allow" });\n      }\n\n      // Preserve explicit role-level denies for least-privilege roles. Admin/Developer are\n      // intentionally canonical full-access system roles and are reset to full access.\n      if (!FULL_ACCESS_SYSTEM_ROLES.has(roleKey)) {\n        for (const deny of currentDenies) {\n          desiredMap.set(String(deny.permissionKey), {\n            permissionKey: String(deny.permissionKey),\n            dataScope: "none",\n            effect: "deny",\n            scopeConfig: deny.scopeConfig ?? null,\n          });\n        }\n      }\n\n      const entries = Array.from(desiredMap.values());\n      return {\n        roleKey,\n        mode: "replace-standard" as const,\n        currentCount: current.length,\n        currentAllowCount: currentAllows.length,\n        desiredCount: entries.length,\n        entries,\n      };\n    });\n\n  const audit = {\n    version: VERSION,\n    mode: APPLY ? "apply" : "dry-run",\n    canonicalPermissionCount: PHASE1_PERMISSION_CATALOG.length,\n    dbActivePermissionCount: dbPermissionKeys.size,\n    missingCatalogPermissions,\n    missingSystemRoles,\n    beforeAllowedByRole: summarize(beforeDirect),\n    plans: plans.map(plan => ({\n      roleKey: plan.roleKey,\n      mode: plan.mode,\n      currentCount: plan.currentCount,\n      desiredCount: plan.desiredCount,\n    })),\n  };\n\n  console.log(`STANDARD_PERMISSION_AUDIT=${JSON.stringify(audit)}`);\n\n  if (!APPLY) {\n    console.log("APPLY_REQUIRED=YES");\n    console.log("DATA_CHANGED=NONE");\n    return;\n  }\n\n  const backupPath = `/tmp/tcrm-system-role-standard-baseline-${Date.now()}.json`;\n  await writeFile(backupPath, JSON.stringify({\n    version: VERSION,\n    createdAt: new Date().toISOString(),\n    directSystemRolePermissions: beforeDirect,\n    protectionHashes: beforeProtection,\n  }, null, 2), "utf8");\n\n  await db.transaction(async (tx: any) => {\n    // Reconcile DB permission catalog with the canonical code catalog.\n    for (const permissionKey of PHASE1_PERMISSION_CATALOG) {\n      const [moduleKey, ...actionParts] = String(permissionKey).split(".");\n      const actionKey = actionParts.join(".") || "view";\n      await tx.execute(sql`\n        INSERT INTO permissions (permission_key, module_key, action_key, name, is_active)\n        VALUES (${String(permissionKey)}, ${moduleKey}, ${actionKey}, ${String(permissionKey)}, 1)\n        ON DUPLICATE KEY UPDATE\n          module_key = VALUES(module_key),\n          action_key = VALUES(action_key),\n          is_active = 1\n      `);\n    }\n\n    const activePermissions = rows(await tx.execute(sql`\n      SELECT id, permission_key AS permissionKey\n      FROM permissions\n      WHERE is_active = 1\n    `));\n    const permissionIdByKey = new Map(activePermissions.map((item: any) => [String(item.permissionKey), Number(item.id)]));\n\n    for (const plan of plans) {\n      if (plan.mode !== "replace-standard") continue;\n      const role = byLegacy.get(plan.roleKey);\n      if (!role) continue;\n      const roleId = Number((role as any).id);\n\n      await tx.execute(sql`DELETE FROM role_permissions WHERE role_id = ${roleId}`);\n\n      for (const entry of plan.entries) {\n        const permissionId = permissionIdByKey.get(String(entry.permissionKey));\n        if (!permissionId) throw new Error(`Permission missing after catalog reconcile: ${entry.permissionKey}`);\n        await tx.execute(sql`\n          INSERT INTO role_permissions\n            (role_id, permission_id, effect, data_scope, scope_config, created_by)\n          VALUES\n            (${roleId}, ${permissionId}, ${entry.effect},\n             ${entry.effect === "deny" ? "none" : entry.dataScope},\n             ${entry.scopeConfig == null ? null : JSON.stringify(entry.scopeConfig)}, NULL)\n        `);\n      }\n\n      await tx.execute(sql`\n        INSERT INTO permission_audit_logs\n          (actor_user_id, target_role_id, action, previous_value, new_value, reason, metadata)\n        VALUES\n          (NULL, ${roleId}, ${VERSION}, NULL, NULL,\n           ${"Apply standard system-role permission baseline"},\n           ${JSON.stringify({ roleKey: plan.roleKey, desiredCount: plan.desiredCount })})\n      `);\n    }\n  });\n\n  const afterDirect = await loadDirectPermissions(db);\n  const afterByRole = groupByRole(afterDirect);\n  const afterProtection = await protectionSnapshot(db);\n\n  const protectionsPass =\n    beforeProtection.customRolePermissions === afterProtection.customRolePermissions &&\n    beforeProtection.userOverrides === afterProtection.userOverrides &&\n    beforeProtection.roleHierarchy === afterProtection.roleHierarchy;\n\n  const preservedRolesPass = Array.from(PRESERVE_CONFIGURED_SYSTEM_ROLES).every(roleKey => {\n    const before = beforeByRole.get(roleKey) || [];\n    if (!before.length) return true;\n    const after = afterByRole.get(roleKey) || [];\n    return snapshotHash(before) === snapshotHash(after);\n  });\n\n  const dangerousViolations: string[] = [];\n  for (const [roleKey, grants] of afterByRole.entries()) {\n    if (FULL_ACCESS_SYSTEM_ROLES.has(roleKey)) continue;\n    for (const item of grants) {\n      if (String(item.effect) === "allow" && dangerousNonAdminGrant(String(item.permissionKey))) {\n        dangerousViolations.push(`${roleKey}:${item.permissionKey}`);\n      }\n    }\n  }\n\n  const finalCatalogRows = rows(await db.execute(sql`\n    SELECT permission_key AS permissionKey\n    FROM permissions\n    WHERE is_active = 1\n  `));\n  const finalKeys = new Set(finalCatalogRows.map((item: any) => String(item.permissionKey)));\n  const catalogPass = PHASE1_PERMISSION_CATALOG.every(key => finalKeys.has(String(key)));\n\n  const afterAllowedByRole = summarize(afterDirect);\n\n  console.log(`BACKUP_PATH=${backupPath}`);\n  console.log(`CATALOG_RECONCILED=${catalogPass ? "YES" : "NO"}`);\n  console.log(`BEFORE_ALLOWED_BY_ROLE=${JSON.stringify(summarize(beforeDirect))}`);\n  console.log(`AFTER_ALLOWED_BY_ROLE=${JSON.stringify(afterAllowedByRole)}`);\n  console.log(`PRESERVED_APPROVED_CORE_ROLES=${preservedRolesPass ? "YES" : "NO"}`);\n  console.log(`CUSTOM_ROLES_UNCHANGED=${beforeProtection.customRolePermissions === afterProtection.customRolePermissions ? "YES" : "NO"}`);\n  console.log(`USER_OVERRIDES_UNCHANGED=${beforeProtection.userOverrides === afterProtection.userOverrides ? "YES" : "NO"}`);\n  console.log(`ROLE_HIERARCHY_UNCHANGED=${beforeProtection.roleHierarchy === afterProtection.roleHierarchy ? "YES" : "NO"}`);\n  console.log(`DANGEROUS_NON_ADMIN_GRANTS=${dangerousViolations.length ? dangerousViolations.join(",") : "NONE"}`);\n\n  if (!catalogPass || !protectionsPass || !preservedRolesPass || dangerousViolations.length) {\n    throw new Error("Standard permission baseline verification failed");\n  }\n\n  console.log("STANDARD_PERMISSION_BASELINE=PASS");\n  console.log("DATA_CHANGED=permissions,role_permissions(system roles only),permission_audit_logs");\n}\n\nmain().catch(error => {\n  console.error(error);\n  process.exit(1);\n});\n',
}

changed = []
for relative, content in FILES.items():
    path = ROOT / relative
    path.parent.mkdir(parents=True, exist_ok=True)
    previous = path.read_text(encoding="utf-8") if path.exists() else None
    if previous == content:
        continue
    path.write_text(content, encoding="utf-8")
    changed.append(relative)

print("PATCH=TCRM-PERMISSIONS-STANDARD-BASELINE-V1")
print("FILES_CHANGED=" + (",".join(changed) if changed else "NONE"))
print("NEXT_DRY_RUN=npx tsx scripts/apply-system-role-standard-baselines.ts")
print("NEXT_APPLY=npx tsx scripts/apply-system-role-standard-baselines.ts --apply")
