Fix3 review: one truncation edge remains. If maxRecords is reached in the middle of a fetched page, truncation is already proven because unconsumed rows remain in that same page. Do not rely on the next-page probe in that case. Return truncated=true immediately when cap is hit before consuming all rows from the current page. Only probe the next page when the cap is reached exactly at the end of the current full page. Add focused tests for both cases. Preserve all LID changes. No git ops.