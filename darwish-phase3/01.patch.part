assName="text-base">{ar ? "عملاء عالي الخطورة" : "High Risk"}</CardTitle></CardHeader><CardContent><p className="text-2xl font-semibold">{supervisorQ.data?.highRiskClients || 0}</p></CardContent></Card>
+        <Card><CardHeader className="pb-2"><CardTitle className="text-base">{ar ? "تنبيهات نشطة" : "Active Alerts"}</CardTitle></CardHeader><CardContent><p className="text-2xl font-semibold">{supervisorQ.data?.activeAlerts || 0}</p><p className="text-xs text-muted-foreground">{ar ? `حرجة: ${supervisorQ.data?.criticalAlerts || 0}` : `critical: ${supervisorQ.data?.criticalAlerts || 0}`}</p></CardContent></Card>
+        <Card><CardHeader className="pb-2"><CardTitle className="text-base">{ar ? "متوسط المخاطر" : "Avg Risk"}</CardTitle></CardHeader><CardContent><p className="text-2xl font-semibold">{Math.round(supervisorQ.data?.avgRiskScore || 0)}</p><p className="text-xs text-muted-foreground">/ 100</p></CardContent></Card>
+        <Card><CardHeader className="pb-2"><CardTitle className="text-base">{ar ? "متوسط الرد" : "Avg Response"}</CardTitle></CardHeader><CardContent><p className="text-2xl font-semibold">{supervisorQ.data?.avgResponseMinutes == null ? "—" : Math.round(supervisorQ.data.avgResponseMinutes)}</p><p className="text-xs text-muted-foreground">{ar ? "دقيقة" : "minutes"}</p></CardContent></Card>
+      </div>
+
+      <div className="grid gap-4 lg:grid-cols-2">
+        <Card><CardHeader><CardTitle>{ar ? "تنبيهات المشرف" : "Supervisor alerts"}</CardTitle><CardDescription>{ar ? "تنبيهات داخلية فقط؛ لا يتم إرسال أي رد أو تنفيذ إجراء تلقائي." : "Internal alerts only; no reply or automatic CRM action is executed."}</CardDescription></CardHeader><CardContent><div className="space-y-2">{alertsQ.data?.length ? alertsQ.data.map((alert) => <div key={alert.id} className="rounded border p-3 text-sm"><div className="flex items-center justify-between gap-2"><strong>{alert.clientName || alert.groupName || alert.groupJid}</strong><Badge variant={alert.severity === "critical" || alert.severity === "high" ? "destructive" : "secondary"}>{alert.severity}</Badge></div><p className="mt-1 font-medium">{alert.title}</p><p className="mt-1 text-muted-foreground">{alert.summary}</p></div>) : <p className="text-sm text-muted-foreground">{ar ? "لا توجد تنبيهات نشطة." : "No active alerts."}</p>}</div></CardContent></Card>
+
+        <Card><CardHeader><CardTitle>{ar ? "أداء مديري الحسابات" : "Account Manager performance"}</CardTitle><CardDescription>{ar ? "مؤشر تشغيلي مبني على المخاطر وزمن الاستجابة، وليس تقييمًا وظيفيًا نهائيًا." : "Operational signal based on risk and response time, not a final HR rating."}</CardDescription></CardHeader><CardContent><div className="space-y-2">{accountManagersQ.data?.length ? accountManagersQ.data.map((item) => <div key={item.accountManagerId} className="flex flex-wrap items-center justify-between gap-3 rounded border p-3 text-sm"><div><strong>{ar ? `مدير حساب #${item.accountManagerId}` : `AM #${item.accountManagerId}`}</strong><div className="text-muted-foreground">{ar ? `عملاء: ${item.clientsMonitored} · عالي الخطورة: ${item.highRiskClients} · تنبيهات: ${item.activeAlerts}` : `clients: ${item.clientsMonitored} · high risk: ${item.highRiskClients} · alerts: ${item.activeAlerts}`}</div></div><div className="text-right"><div className="text-xl font-semibold">{item.serviceQualityScore}/100</div><div className="text-xs text-muted-foreground">{item.avgResponseMinutes == null ? "—" : `${Math.round(item.avgResponseMinutes)} min`}</div></div></div>) : <p className="text-sm text-muted-foreground">{ar ? "لا توجد بيانات كافية بعد." : "Not enough data yet."}</p>}</div></CardContent></Card>
+      </div>
+
+      <Card><CardHeader><CardTitle>{ar ? "ملخص الإدارة اليومي" : "Daily management digest"}</CardTitle></CardHeader><CardContent><div className="grid gap-3 md:grid-cols-5 text-sm"><div><strong>{digestQ.data?.analyses || 0}</strong><div className="text-muted-foreground">{ar ? "تحليل" : "analyses"}</div></div><div><strong>{digestQ.data?.complaints || 0}</strong><div className="text-muted-foreground">{ar ? "شكوى" : "complaints"}</div></div><div><strong>{digestQ.data?.urgent || 0}</strong><div className="text-muted-foreground">{ar ? "عاجل" : "urgent"}</div></div><div><strong>{digestQ.data?.escalations || 0}</strong><div className="text-muted-foreground">{ar ? "تصعيد" : "escalations"}</div></div><div><strong>{digestQ.data?.negative || 0}</strong><div className="text-muted-foreground">{ar ? "سلبي" : "negative"}</div></div></div>{digestQ.data?.topIssueCategories?.length ? <div className="mt-4 flex flex-wrap gap-2">{digestQ.data.topIssueCategories.map((item) => <Badge key={item.category} variant="secondary">{item.category} · {item.count}</Badge>)}</div> : null}</CardContent></Card>
+
       <Card><CardHeader><CardTitle>{ar ? "ربط مجموعة بعميل" : "Link a group to a client"}</CardTitle><CardDescription>{ar ? "يُستخدم الربط لتحديد سياق CRM للأحداث الواردة." : "The mapping supplies CRM context for inbound events."}</CardDescription></CardHeader><CardContent className="space-y-4">
         <div className="grid gap-4 md:grid-cols-2"><div><Label>{ar