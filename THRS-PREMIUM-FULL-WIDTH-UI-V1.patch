diff --git a/client/src/pages/THRSPage.tsx b/client/src/pages/THRSPage.tsx
index 71c377d..thrsui1 100644
--- a/client/src/pages/THRSPage.tsx
+++ b/client/src/pages/THRSPage.tsx
@@ -179,18 +179,18 @@ function KpiCard({
   icon: any;
 }) {
   return (
-    <Card className="rounded-3xl border bg-card/80 shadow-sm">
-      <CardContent className="flex items-center justify-between gap-4 p-5">
-        <div>
-          <p className="text-xs font-bold uppercase tracking-[0.18em] text-muted-foreground">
+    <Card className="group overflow-hidden rounded-2xl border border-slate-200/70 bg-card shadow-[0_10px_30px_rgba(15,23,42,0.04)] transition-all duration-200 hover:-translate-y-0.5 hover:shadow-[0_16px_40px_rgba(15,23,42,0.08)] dark:border-slate-800/80">
+      <CardContent className="flex min-h-[132px] items-center justify-between gap-5 p-6">
+        <div className="min-w-0">
+          <p className="text-[11px] font-extrabold uppercase tracking-[0.16em] text-muted-foreground">
             {title}
           </p>
-          <p className="mt-2 text-2xl font-black">{value}</p>
+          <p className="mt-2 text-[1.75rem] font-black tracking-tight">{value}</p>
           {hint && <p className="mt-1 text-xs text-muted-foreground">{hint}</p>}
         </div>
-        <div className="rounded-2xl bg-primary/10 p-3 text-primary">
+        <div className="rounded-2xl border border-primary/10 bg-primary/10 p-3.5 text-primary transition-transform duration-200 group-hover:scale-105">
           <Icon className="h-5 w-5" />
         </div>
       </CardContent>
@@ -428,29 +428,29 @@ export default function THRSPage() {
   return (
     <CRMLayout>
       <div
-        className="mx-auto max-w-7xl space-y-6 p-4 sm:p-6"
+        className="w-full max-w-none space-y-5 px-5 py-5 sm:px-6 lg:px-8 xl:px-10 2xl:px-12"
         dir={isRTL ? "rtl" : "ltr"}
       >
-        <Card className="overflow-hidden rounded-[2rem] border-0 bg-gradient-to-br from-primary/15 via-background to-muted/50 shadow-sm">
-          <CardContent className="grid gap-6 p-6 lg:grid-cols-[1.4fr_.8fr] lg:items-center">
+        <Card className="overflow-hidden rounded-3xl border border-slate-200/70 bg-card shadow-[0_14px_44px_rgba(15,23,42,0.06)] dark:border-slate-800/80">
+          <CardContent className="grid gap-6 p-6 lg:p-7 xl:grid-cols-[minmax(0,1fr)_420px] xl:items-center">
             <div>
               <Badge
-                variant="secondary"
-                className="mb-4 rounded-full px-3 py-1"
+                variant="outline"
+                className="mb-4 rounded-full border-primary/15 bg-primary/5 px-3 py-1 font-bold text-primary"
               >
-                THRS · TCRM
+                THRS Work Hub
               </Badge>
-              <h1 className="text-3xl font-black tracking-tight sm:text-4xl">
-                {isRTL ? "مركز THRS داخل TCRM" : "THRS Work Hub inside TCRM"}
+              <h1 className="text-3xl font-black tracking-[-0.035em] sm:text-4xl">
+                {isRTL ? "مركز العمل THRS" : "THRS Work Hub"}
               </h1>
-              <p className="mt-3 max-w-3xl text-sm leading-7 text-muted-foreground">
+              <p className="mt-3 max-w-2xl text-sm leading-7 text-muted-foreground">
                 {isRTL
-                  ? "حضور وانصراف، Heartbeat، طلبات، موافقات داخلية، وربط نهائي مع HR System بنفس فلسفة TOS."
-                  : "Attendance, heartbeat, requests, internal approval, and final HR System sync with the same TOS workflow philosophy."}
+                  ? "الحضور والانصراف، متابعة ساعات العمل، الطلبات والموافقات، وربط HR في مساحة عمل واحدة."
+                  : "Attendance, work hours, requests, approvals, and HR sync in one focused workspace."}
               </p>
               <div className="mt-5 flex flex-wrap gap-2">
                 <Badge className="rounded-full bg-emerald-600 text-white">
@@ -468,8 +468,8 @@ export default function THRSPage() {
                 </Badge>
               </div>
             </div>
-            <Card className="rounded-3xl bg-background/80 shadow-sm">
-              <CardContent className="space-y-4 p-5">
+            <Card className="rounded-2xl border border-slate-200/80 bg-background shadow-[0_10px_28px_rgba(15,23,42,0.05)] dark:border-slate-800">
+              <CardContent className="space-y-4 p-6">
                 <div className="flex items-center justify-between gap-3">
                   <div>
                     <p className="text-xs font-bold uppercase tracking-[0.18em] text-muted-foreground">
@@ -487,7 +487,7 @@ export default function THRSPage() {
                   </div>
                   <span
                     className={cn(
-                      "rounded-2xl p-3",
+                      "rounded-2xl border p-3",
                       isInside
-                        ? "bg-emerald-100 text-emerald-700"
-                        : "bg-slate-100 text-slate-600"
+                        ? "border-emerald-200 bg-emerald-50 text-emerald-700"
+                        : "border-slate-200 bg-slate-50 text-slate-600 dark:border-slate-800 dark:bg-slate-900"
                     )}
                   >
                     <HeartPulse className="h-6 w-6" />
@@ -495,14 +495,14 @@ export default function THRSPage() {
                   </span>
                 </div>
                 <div className="grid grid-cols-2 gap-3 text-sm">
-                  <div className="rounded-2xl border p-3">
+                  <div className="rounded-2xl border border-slate-200/80 bg-muted/15 p-3.5 dark:border-slate-800">
                     <p className="text-muted-foreground">
                       {isRTL ? "الحضور" : "Check-in"}
                     </p>
                     <p className="mt-1 font-black">
                       {fmtTime(todayAttendance?.checkInAt, isRTL)}
                     </p>
                   </div>
-                  <div className="rounded-2xl border p-3">
+                  <div className="rounded-2xl border border-slate-200/80 bg-muted/15 p-3.5 dark:border-slate-800">
                     <p className="text-muted-foreground">
                       {isRTL ? "الانصراف" : "Check-out"}
                     </p>
@@ -511,7 +511,7 @@ export default function THRSPage() {
                   </div>
                 </div>
                 <div className="grid grid-cols-2 gap-2">
                   <Button
-                    className="rounded-2xl"
+                    className="h-11 rounded-xl font-bold shadow-sm"
                     variant="default"
                     onClick={() => checkin.mutate()}
                     disabled={isInside || checkin.isPending}
@@ -521,7 +521,7 @@ export default function THRSPage() {
                     {isRTL ? "تسجيل حضور" : "Check in"}
                   </Button>
                   <Button
-                    className="rounded-2xl"
+                    className="h-11 rounded-xl font-bold"
                     variant={isInside ? "destructive" : "outline"}
                     onClick={() => checkout.mutate()}
                     disabled={!isInside || checkout.isPending}
@@ -534,7 +534,7 @@ export default function THRSPage() {
           </CardContent>
         </Card>
 
-        <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">
+        <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5">
           <KpiCard
             title={isRTL ? "أيام الحضور" : "Attendance days"}
             value={summary.attendanceDays || 0}
@@ -559,20 +559,20 @@ export default function THRSPage() {
         </div>
 
-        <Tabs defaultValue="dashboard" className="space-y-4">
-          <TabsList className="h-auto flex-wrap rounded-2xl border bg-muted/40 p-1">
-            <TabsTrigger value="dashboard" className="rounded-xl px-4 py-2">
+        <Tabs defaultValue="dashboard" className="space-y-5">
+          <TabsList className="h-auto w-full flex-wrap justify-start gap-1 rounded-2xl border border-slate-200/80 bg-card p-1.5 shadow-sm dark:border-slate-800 lg:w-auto">
+            <TabsTrigger value="dashboard" className="rounded-xl px-5 py-2.5 font-semibold data-[state=active]:shadow-sm">
               {isRTL ? "لوحة الموظف" : "Dashboard"}
             </TabsTrigger>
-            <TabsTrigger value="requests" className="rounded-xl px-4 py-2">
+            <TabsTrigger value="requests" className="rounded-xl px-5 py-2.5 font-semibold data-[state=active]:shadow-sm">
               {isRTL ? "الطلبات" : "Requests"}
             </TabsTrigger>
-            <TabsTrigger value="team" className="rounded-xl px-4 py-2">
+            <TabsTrigger value="team" className="rounded-xl px-5 py-2.5 font-semibold data-[state=active]:shadow-sm">
               {isRTL ? "الفريق" : "Team"}
             </TabsTrigger>
             {canManageSettings && (
-              <TabsTrigger value="settings" className="rounded-xl px-4 py-2">
+              <TabsTrigger value="settings" className="rounded-xl px-5 py-2.5 font-semibold data-[state=active]:shadow-sm">
                 {isRTL ? "الإعدادات" : "Settings"}
               </TabsTrigger>
             )}
@@ -581,8 +581,8 @@ export default function THRSPage() {
 
           <TabsContent value="dashboard" className="space-y-4">
-            <div className="grid gap-4 lg:grid-cols-[.9fr_1.1fr]">
-              <Card className="rounded-3xl">
+            <div className="grid gap-5 xl:grid-cols-[minmax(0,1.2fr)_minmax(380px,.8fr)]">
+              <Card className="rounded-2xl border border-slate-200/80 shadow-[0_10px_30px_rgba(15,23,42,0.04)] dark:border-slate-800">
                 <CardHeader>
                   <CardTitle>
                     {isRTL ? "تقويم الحضور" : "Attendance calendar"}
@@ -612,7 +612,7 @@ export default function THRSPage() {
                               onClick={() => setSelectedDate(day.key)}
                               onKeyDown={event => { if (event.key === "Enter" || event.key === " ") setSelectedDate(day.key); }}
                               className={cn(
-                                "min-h-16 cursor-pointer rounded-2xl border p-2 text-center text-xs transition hover:scale-[1.02]",
+                                "min-h-20 cursor-pointer rounded-xl border p-2.5 text-center text-xs transition-all hover:-translate-y-0.5 hover:shadow-sm",
                                 cls,
                                 selectedDate === day.key && "ring-2 ring-primary"
                               )}
@@ -647,7 +647,7 @@ export default function THRSPage() {
                 </CardContent>
               </Card>
 
-              <Card className="rounded-3xl">
+              <Card className="rounded-2xl border border-slate-200/80 shadow-[0_10px_30px_rgba(15,23,42,0.04)] dark:border-slate-800">
                 <CardHeader>
                   <CardTitle>
                     {isRTL ? `تفاصيل يوم ${selectedDate}` : `Selected day · ${selectedDate}`}
@@ -660,7 +660,7 @@ export default function THRSPage() {
                   </CardDescription>
                 </CardHeader>
                 <CardContent className="grid gap-3 sm:grid-cols-2">
-                  <div className="rounded-2xl border p-4">
+                  <div className="rounded-xl border border-slate-200/80 bg-muted/15 p-4 dark:border-slate-800">
                     <p className="text-sm text-muted-foreground">
                       {isRTL ? "مصدر الحضور" : "Check-in source"}
                     </p>
@@ -668,7 +668,7 @@ export default function THRSPage() {
                       {selectedAttendance?.checkInSource || "—"}
                     </p>
                   </div>
-                  <div className="rounded-2xl border p-4">
+                  <div className="rounded-xl border border-slate-200/80 bg-muted/15 p-4 dark:border-slate-800">
                     <p className="text-sm text-muted-foreground">
                       {isRTL ? "مصدر الانصراف" : "Check-out source"}
                     </p>
@@ -676,7 +676,7 @@ export default function THRSPage() {
                       {selectedAttendance?.checkOutSource || "—"}
                     </p>
                   </div>
-                  <div className="rounded-2xl border p-4">
+                  <div className="rounded-xl border border-slate-200/80 bg-muted/15 p-4 dark:border-slate-800">
                     <p className="text-sm text-muted-foreground">
                       {isRTL ? "آخر ظهور" : "Last seen"}
                     </p>
@@ -684,7 +684,7 @@ export default function THRSPage() {
                       {fmtTime(selectedAttendance?.lastSeenAt, isRTL)}
                     </p>
                   </div>
-                  <div className="rounded-2xl border p-4">
+                  <div className="rounded-xl border border-slate-200/80 bg-muted/15 p-4 dark:border-slate-800">
                     <p className="text-sm text-muted-foreground">
                       {isRTL ? "مدة العمل" : "Work duration"}
                     </p>
@@ -692,7 +692,7 @@ export default function THRSPage() {
                       {minutesLabel(selectedAttendance?.totalMinutes, isRTL)}
                     </p>
                   </div>
-                  <div className="rounded-2xl border p-4 sm:col-span-2">
+                  <div className="rounded-xl border border-slate-200/80 bg-muted/15 p-4 sm:col-span-2 dark:border-slate-800">
                     <p className="text-sm text-muted-foreground">
                       {isRTL ? "طلبات هذا اليوم" : "Requests on this day"}
                     </p>
